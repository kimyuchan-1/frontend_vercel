# ============================================================================
# Environment Variables Template
# ============================================================================
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control - it contains sensitive data
# ============================================================================

# ============================================================================
# Backend API Configuration
# ============================================================================
# The base URL for your backend API server
# 
# Development: Use localhost or ngrok URL for local development
# Production: Use your production API URL (e.g., https://api.yourdomain.com)
#
# IMPORTANT: This is a SERVER-ONLY variable (no NEXT_PUBLIC_ prefix)
# It will NOT be exposed to the browser and is only accessible in:
# - API routes (src/app/api/**/route.ts)
# - Server components
# - Server-side code
#
# Examples:
#   Development: http://localhost:8080
#   Development (ngrok): https://your-subdomain.ngrok-free.dev:8080
#   Production: https://api.yourdomain.com
BACKEND_URL=http://localhost:8080

# ============================================================================
# Supabase Configuration
# ============================================================================
# Get these values from your Supabase project dashboard:
# https://app.supabase.com/project/YOUR_PROJECT/settings/api

# Supabase Project URL
# This is your unique Supabase project URL
# 
# SAFE FOR BROWSER: This variable uses NEXT_PUBLIC_ prefix, which means
# it will be embedded in the client-side JavaScript bundle and is safe
# to expose to browsers.
#
# Example: https://abcdefghijklmnop.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Service Role Key (Admin Key)
#
# ⚠️  CRITICAL SECURITY WARNING ⚠️
# This key has FULL ADMINISTRATIVE ACCESS to your Supabase database!
# It can bypass Row Level Security (RLS) policies and perform any operation.
#
# NEVER prefix this with NEXT_PUBLIC_ or it will be exposed to browsers!
# NEVER commit this key to version control!
# NEVER share this key in public forums, logs, or error messages!
#
# This is a SERVER-ONLY variable and should ONLY be used in:
# - API routes (src/app/api/**/route.ts)
# - Server components
# - Server-side code that requires admin privileges
#
# For client-side Supabase operations, use the anon key instead (see below)
#
# Example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Supabase Anonymous Key (Public Key) - OPTIONAL
# This is the public key that can be safely used in client-side code
# It respects Row Level Security (RLS) policies
#
# SAFE FOR BROWSER: This variable uses NEXT_PUBLIC_ prefix
# Uncomment and add this if you need client-side Supabase access
#
# Example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================================================
# Node Environment (Optional)
# ============================================================================
# Specifies the runtime environment
# Valid values: development, production, test
# Defaults to 'development' if not specified
#
# This is automatically set by Next.js in most cases, but you can override it
# NODE_ENV=development

# ============================================================================
# Additional Configuration (Optional)
# ============================================================================
# Add any additional environment variables your application needs below
# Remember: Use NEXT_PUBLIC_ prefix ONLY for variables that are safe to expose
# to the browser. All other variables should be server-only.
NODE_ENV=development